<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cotizaciones</title>
  <link rel="stylesheet" href="/estilos.css" />
</head>

<body>
  <main style="padding: 2rem; max-width: 2000px; margin:auto;"></main>

  <!-- NAVBAR -->
  <div class="nav">
    <a href="/catalogo">Catálogo</a>
    <a href="/entradas">Entradas</a>
    <a href="/inventario">Inventario</a>
    <a href="/salidas">Salidas</a>
    <a href="/clientes">Clientes</a>
    <a href="/cotizaciones" class="active">Cotizaciones</a>

    <% if (usuario && usuario.tipo_usuario==='administrador' ) { %>
      <a href="/usuarios">Usuarios</a> <!-- Solo visible para administradores -->
      <% } %>

  </div>

  <!-- CONTENEDOR PRINCIPAL -->
  <main class="contenedor">
    <h2>Listado de Cotizaciones</h2>

    <!-- Formulario de búsqueda -->
    <form action="/cotizaciones/buscar" method="GET" style="margin-bottom: 1rem;">
      <label for="folio_buscar">Buscar por Folio:</label>
      <input type="text" name="folio_buscar" id="folio_buscar" required placeholder="SLM-001 o 25">
      <button type="submit" class="btn btn-secundario">Buscar</button>
      <a href="/cotizaciones" class="btn btn-exito">Mostrar todas</a>
    </form>

    <a href="/cotizaciones/nueva" class="btn btn-exito">Agregar nueva cotización</a>

    <table>
      <thead>
        <tr>
          <th>Folio</th>
          <th>Fecha de Folio</th>
          <th>Partidas Cotizadas</th>
          <th>Monto Máx. Cotizado</th>
          <th>Dependencia</th>
          <th>Vigencia</th>
          <th>Fecha Fin</th>
          <th>Responsable</th>
          <th>Estatus</th>
          <th>Partidas Asignadas</th>
          <th>Monto Máx. Asignado</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% cotizaciones.forEach(i=> { %>
          <tr>
            <!-- 👇 Si hay folio alfanumérico lo muestra, si no, el consecutivo -->
            <td>
              <%= i.folioVisible || i.noDeFolioFK || '—' %>
            </td>

            <td>
              <%= i.fechaDeFolio ? new Date(i.fechaDeFolio).toLocaleDateString('es-MX') : '' %>
            </td>
            <td>
              <%= i.partidasCotizadas %>
            </td>
            <td>$<%= Number(i.montoMaxCotizado || 0).toFixed(2) %>
            </td>
            <td>
              <%= i.dependencia || '' %>
            </td>
            <td>
              <%= (i.vigenciaDeLaCotizacion ?? '' ) !=='' ? (i.vigenciaDeLaCotizacion + ' días' ) : 'Sin vigencia' %>
            </td>
            <td>
              <%= i.fechaFinDeLaCotizacion ? new Date(i.fechaFinDeLaCotizacion).toLocaleDateString('es-MX')
                : 'Sin vigencia' %>
            </td>
            <td>
              <%= i.responsableDeLaCotizacion || '' %>
            </td>
            <td>
              <%= i.estatusDeLaCotizacion %>
            </td>
            <td>
              <%= i.partidasAsignadas %>
            </td>
            <td>$<%= Number(i.montoMaximoAsignado || 0).toFixed(2) %>
            </td>
            <td>
              <div style="display:flex; gap:1rem; justify-content:center;">
                <a href="/cotizaciones/editar/<%= i.id %>" class="btn btn-secundario">Editar</a>
                <form action="/cotizaciones/eliminar/<%= i.id %>" method="GET" style="margin:0;"
                  onsubmit="return confirm('¿Seguro que deseas eliminar esta cotización? Esta acción no se puede deshacer.');">
                  <button type="submit" class="btn btn-danger">Eliminar</button>
                </form>
              </div>
            </td>
          </tr>
          <% }) %>
      </tbody>
    </table>
  </main>

  <form action="/logout" method="get">
    <a href="/logout" class="btn-logout">Cerrar sesión</a>
  </form>
</body>

</html>