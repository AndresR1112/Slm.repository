<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= editar ? 'Editar Usuario' : 'Nuevo Usuario' %></title>
  <link rel="stylesheet" href="/estilos.css">
</head>

<body>
    <main style="padding: 2rem; max-width: 2000px; margin:auto;"></main>
 
  <main class="contenedor" style="padding: 2rem;">
    <h2><%= editar ? 'Editar Usuario' : 'Agregar Nuevo Usuario' %></h2>

    <% if (error) { %>
      <p style="color:red;"><%= error %></p>
    <% } %>

    <form action="<%= editar ? '/usuarios/editar/' + empleado.Id : '/usuarios/nuevo' %>" method="POST"
      style="display:flex; flex-direction: column; gap: 1rem; background-color: white; padding: 2rem; border-radius: 10px;">

      <label>
        Usuario:
        <input type="text" name="usuario" class="input" required
          value="<%= empleado.usuario || '' %>">
      </label>

      <label>
        Nombre completo:
        <input type="text" name="nombre_completo" class="input" required
          value="<%= empleado.nombre_completo || '' %>">
      </label>

      <label>
        Puesto:
        <input type="text" name="puesto" class="input" required
          value="<%= empleado.puesto || '' %>">
      </label>

      <label>
        Teléfono:
        <input type="text" name="telefono" class="input" maxlength="10" required
          value="<%= empleado.telefono || '' %>">
      </label>

      <label>
        Correo:
        <input type="email" name="correo" class="input" required
          value="<%= empleado.correo || '' %>">
      </label>

      <label>
        Contraseña:
        <input type="password" name="password" class="input" maxlength="8" required
          value="<%= empleado.password || '' %>">
      </label>

      <div style="display:flex; gap: 1rem;">
        <button type="submit" class="btn btn-exito">
          <%= editar ? 'Actualizar Usuario' : 'Añadir Usuario' %>
        </button>
        <a href="/usuarios" class="btn btn-secundario">Cancelar</a>
      </div>
    </form>
  </main>
</body>
</html>
